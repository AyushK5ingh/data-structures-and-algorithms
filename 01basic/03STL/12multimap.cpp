#include <bits/stdc++.h>
using namespace std;

void expainmultimap()
{
    //everything sam as map, only it can store multiple keys
    //omly mpp[keys] will not work, need to use mpp.equal_range(key)
    //equal_range() returns a pair of iterators that represent the range of elements with the specified key
    //it returns a pair of iterators that represent the range of elements with the specified key
    //it is used to find the range of elements with the specified key in the multimap
}
int main() {
    cout << "Hello, World!" << endl;
    return 0;
}